---
title: "HW1讲评"
author: "**凌晓光**"
institute: "**南京大学**"
documentclass: ctexart
output:
  rticles::ctex:
    fig_caption: yes
    number_sections: yes
    toc: yes
classoption: "hyperref,"
---

# 主要的问题

*再次强调，不要抄袭答案，引用答案你得注明啊




*有些题目确实不太容易揣摩题意，大家要以数据结果为依据，自圆其说地论述即可。

比如Q5，baseline variable hindx不平稳，这表这表明实验前的分组并不是随机的，因此这个实验不太理想。
而另一方面，cholest又与hindex矛盾。所以这个实验很难清晰地描述。

*用Markdown写公式美观而且简洁，服从分布的符号用 \sim 表示，比如 $X \sim N(\mu,\sigma^2)$
$$t=\frac{Y_1-Y_0}{\sqrt{\frac{(n_1-1)s^2_1+(n_0-1)s^2_0}{n_1+n_0-2}(\frac{1}{n_1}+\frac{1}{n_0})}}$$
*我们为什么用rmarkdown、markstat? 因为好用啊。。。谢益辉说过，想让大家做对，最有效的办法是让做对比做错更容易。



#操作题


*我们既要关注绝对值的差异，也要关注差异的显著性水平。目前的情况是，后者往往更主流。

Q5 我们可以从绝对值上阐述hindx\cholest的变化，并做出合理解释；
但也要指出前者在处理前即有显著差异，而后者前后均无显著差异，



*部分同学对因果推断理解不太好







*答案做法：dplyr+管道符号
```{r}
library(haven)
rand <- read_dta("C:/Users/Xiaoguang/Desktop/rand.dta")
library(dplyr)
rand %>% group_by(any_ins)%>%
  summarise("mean(ghindxx)"=mean(rand$ghindxx,na.rm=T),
            "sd(ghindxx)"=sd(rand$ghindxx),"n(ghindxx)"=n())
```



library(foreign)
library(psych)
dt = read.dta("C:/Users/Administrator/Desktop/rand.dta")
res = with(dt, describeBy(dt, any_ins))
res0 = res[[1]][c(1,3,4,6,7) ,c(2,3,4,5,8,9,10)]
res1 = res[[2]][c(1,3,4,6,7) ,c(2,3,4,5,8,9,10)]
res0



```{r,results='asis'}

library(haven)
rand <- read_dta("C:/Users/Xiaoguang/Desktop/rand.dta")
vars <- c('female','blackhisp','educper','ghindx','cholest')
msl <- function(x, na.omit=FALSE){
 if (na.omit)
   x <- x[!is.na(x)]
 m <- mean(x)
 s <- sd(x)
 n <- length(x)
 return(c(n=n,mean=m,sd=s))
 }
ms <- function(x)sapply(x,msl, na.omit=TRUE)
fd <- by(rand[vars], rand$any_ins, ms)
library(stargazer)
stargazer(fd$`0`,fd$`1`,header=FALSE)

```


newdata <-as.data.frame(RAND_data)
library(dplyr)
RAND_small <- select(newdata, female, blackhisp, educper, ghindx, cholest, any_ins)
newdata_1 <- subset(RAND_small, any_ins==1)
newdata_11 <- select(newdata_1, female, blackhisp, educper, ghindx, cholest)
newdata_0 <- subset(RAND_small, any_ins==0)
newdata_00 <- select(newdata_0, female, blackhisp, educper, ghindx, cholest)
library(stargazer)
stargazer(newdata_11, title="any ins=1", header = FALSE, min.max = FALSE)





一个错误的2 samples 均值t test：注意，两组样本均值差作为新随机变量服从t分布，我们不能分组直接作差，
而是分别求均值后作差，检验均值是否与0无差异。
```{r}
library(haven)
rand <- read_dta("C:/Users/Xiaoguang/Desktop/rand.dta")

newdata <- na.omit(rand)
T_gh <- subset(newdata$ghindx, newdata$any_ins == 1)
F_gh <- subset(newdata$ghindx, newdata$any_ins == 0)

```

                         





tabstat female blackhisp educper ghindx cholest,
>  by(any_ins) stat(mean sd n)












LaTeX包[**ctex**](http://ctan.org/pkg/ctex)支持若干种字体选项，如果你是**ctex**老用户，请注意这里我们要求的最低版本是2.2，你可能需要升级你的LaTeX包。从版本2.0开始，**ctex**支持根据不同操作系统自动选择中文字体，简直是为人类进步作出了巨大贡献，我们再也不必费尽口舌向用户解释“啊，你用Windows啊，那么你该使用什么字体；啊，你用Mac啊，又该如何如何”。

下面的YAML元数据应该能满足多数用户的需求，主要设置两项参数：文档类为`ctexart`（当然也可以是别的类），输出格式为`rticles::ctex`，其默认LaTeX引擎为XeLaTeX（真的，别纠结你的旧爱PDFLaTeX了）。

```yaml
---
documentclass: ctexart
output: rticles::ctex
---
```

`rticles::ctex`的参数都是普通的`pdf_document`参数，参见文档**rmarkdown**包的文档，这里就不赘述了。

Windows和Mac用户应该都已经有自带的中文字体了。Linux用户可以另外下载 [Fandol字体](http://ctan.org/pkg/fandol)^[http://ctan.org/tex-archive/fonts/fandol]，它号称是免费的，不过我们也没太搞清楚它的来头。把那些.otf字体文件下载下来保存到`~/.fonts`文件夹下，然后命令行切换到那里，运行`fc-cache -fv`。

# R代码段

R代码用R Markdown的语法嵌入，即三个反引号开始一段代码```` ```{r} ````和三个反引号```` ``` ```` 结束一段代码：

```{r}
options(digits = 4)
fit = lm(dist ~ speed, data = cars)
coef(summary(fit))
b = coef(fit)
```

上面回归方程中的斜率是`r b[2]`，完整的回归方程为：$$ Y = `r b[1]` + `r b[2]`x$$

画图当然也是木有问题的啦，想画就说嘛，不说我怎么知道你想画呢？

```{r scatter, fig.cap='cars数据散点图以及回归直线。'}
par(mar = c(4, 4, .1, .1), las = 1)
plot(cars, pch = 19)
abline(fit, col = 'red')
```

请不要问我为什么图浮动到下一页去了，这么初级的LaTeX问题问出来信不信我扁你。

# 源代码控

这里提供的rticles模板可能由于种种原因不能满足客官的要求，LaTeX用户就是这样永无止境地调格式（唉，跟Word用户到底有啥区别呢）。若真是需要调整，你可以复制一份默认模板去改，如前面所说，本文档的模板是`rticles::ctex_template()`，它是一个文本文件。若熟悉LaTeX的话一看就明白，只不过里面有些Pandoc变量而已；若不熟悉LaTeX我们在这里说了也白说，花几天时间好好啃一啃LaTeX入门手册吧。

本文档所用的模板是从Pandoc默认LaTeX模板基础上做了少许改动而来的：https://github.com/yihui/pandoc-templates/blob/ctex/default.latex 具体改动从GIT提交消息日志中可以看到，主要就是去掉了mathspec包和hyperref包。

# 小结

事实证明我们可以理直气壮地通过XeLaTeX将中文R Markdown转化为PDF文档，麻麻再也不用担心我的论文满屏幕都是反斜杠，朕养完小白鼠之后终于不必先折腾三个小时LaTeX再开始写实验报告了：打开RStudio，菜单File > New File > R Markdown，然后从模板中选择CTeX Documents，搞定。
